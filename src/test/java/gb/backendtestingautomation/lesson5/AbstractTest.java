package gb.backendtestingautomation.lesson5;import gb.backendtestingautomation.lesson6.db.dao.ProductsMapper;import gb.backendtestingautomation.lesson6.db.model.ProductsExample;import org.apache.ibatis.io.Resources;import org.apache.ibatis.session.SqlSession;import org.apache.ibatis.session.SqlSessionFactory;import org.apache.ibatis.session.SqlSessionFactoryBuilder;import java.io.IOException;import java.io.InputStream;public abstract class AbstractTest {    static ProductsExample example;    static ProductsMapper productsMapper;    static {        // Create sql session and an example of Products for testing DB:        String resource = "mybatis-config.xml";        InputStream is = null;        try {            is = Resources.getResourceAsStream(resource);        } catch (IOException e) {            e.printStackTrace();        }        SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(is);        SqlSession session = sqlSessionFactory.openSession();        productsMapper = session.getMapper(ProductsMapper.class);        example = new ProductsExample();    }}